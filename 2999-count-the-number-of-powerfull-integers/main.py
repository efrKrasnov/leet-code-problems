class Solution:
    def numberOfPowerfulInt(self, start: int, finish: int, limit: int, s: str) -> int:
        result = 0
        
        s_int = int(s)
        power = len(s)
        while s_int < finish:
            
       
            
        return result